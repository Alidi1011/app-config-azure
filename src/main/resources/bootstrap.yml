spring:
  cloud:
    azure:
      appconfiguration:
        stores:
          -  connection-string: ${CONNECTION}
             selects:
               - key-filter: /${spring.application.name}/
  application:
    name: app-config-azure
  profiles:
    active: dev
management:
  endpoints:
    web:
      exposure:
        include: "*"